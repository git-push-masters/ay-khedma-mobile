import 'package:flutter/material.dart';

import 'widgets/search_location_view_body.dart';

class SearchAboutLocationView extends StatelessWidget {
  const SearchAboutLocationView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:SearchAboutLocationViewBody() ,
    );
  }
}